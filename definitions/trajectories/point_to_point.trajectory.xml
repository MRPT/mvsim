<!--
  Exactly-reproducible-trajectory snippet: a single, non-looping S-curve run
  from (0,0) to (12,4), taking 16s, after which the vehicle comes to a full
  stop and stays there ("play once"). Intended to be <include>d inside a
  <controller class="trajectory"> node which should set loop="false"
  (see demo_trajectory.world.xml).
-->
<waypoint t="0.0"  x="0.0"  y="0.0" />
<waypoint t="6.0"  x="4.0"  y="0.0" />
<waypoint t="10.0" x="8.0"  y="2.0" />
<waypoint t="16.0" x="12.0" y="4.0" />
